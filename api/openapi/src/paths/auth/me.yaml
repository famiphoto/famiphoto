get:
  operationId: getMe
  summary: サインインしている自身の情報を取得します。
  security:
    - cookieAuth: []
  response:
    "200":
      description: 自身の情報
      content:
        application/json:
          schema:
            $ref: "../../openapi.yaml#/components/schemas/MeResponse"
