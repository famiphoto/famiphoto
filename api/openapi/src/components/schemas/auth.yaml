SignInResponse:
  type: object
  properties: 
    myId: 
      type: string
      x-oapi-codegen-extra-tags:
        validate: "required"
    password:
      type: string
      x-oapi-codegen-extra-tags:
        validate: "required"
  required: 
    - myId
    - password

MeResponse:
  type: object
  properties:
    userId:
      type: integer
    myId:
      type: string
    isAdmin:
      type: bool
  required:
    - userId
    - myId
    - isAdmin
