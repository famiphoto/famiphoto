GetPhotoListResponse:
  properties:
    items:
      type: array
      items: 
        $ref: "../../openapi.yaml#/components/schemas/GetPhotoListItem"
    total: 
      type: integer
  required:    
    - items
    - total

GetPhotoListItem:
  properties:
    photoId:
      type: integer
  required:
    - photoId

GetPhotoResponse:
  properties:
    photoId:
      type: integer
    photoName: 
        type: string
  required: 
    - photoId
    - photoName
